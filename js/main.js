const topbar=document.querySelector('.top');const menu=document.querySelector('.menu');if(menu){menu.addEventListener('click',()=>topbar.classList.toggle('open'))}const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')})},{threshold:.15});document.querySelectorAll('.reveal,.cards article,.productGrid article').forEach(el=>{el.classList.add('reveal');io.observe(el)});
